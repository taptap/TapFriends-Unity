stages:
  - build
  - test

test:
  tags:
    - ios-builder
  stage: test
  script:
    - ./.ci/test.sh

build:
  tags:
    - ios-builder
  stage: build
  script:
    - ./.ci/build.sh
  only:
    - merge_requests
      
  

